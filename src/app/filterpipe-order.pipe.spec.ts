import { FilterpipeOrderPipe } from './filterpipe-order.pipe';

describe('FilterpipeOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpipeOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
